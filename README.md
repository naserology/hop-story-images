# HOP story images

Public image host for HOP's Instagram Story automation.

Instagram's Graph API will not accept a file upload — it needs a public URL
its servers can fetch. Approved story PNGs are pushed here and referenced by
their `raw.githubusercontent.com` URL.

These are marketing images that go on a public Instagram account. Nothing
private belongs in this repo.
